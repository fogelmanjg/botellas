name: botellas-frontend

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - "127.0.0.1:4201:4201"
    volumes:
      - .:/app
      - /app/node_modules
      - /app/.angular
    environment:
      NODE_ENV: development
